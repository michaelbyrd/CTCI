# Chapter 2 | Linked Lists
# Problem 2.5 — Sum Lists
#
# Two numbers are represented by linked lists where each node contains a
# single digit stored in reverse order (ones digit first).
# Add the two numbers and return the sum as a linked list in the same format.
#
# Example:
#   Input:  7 -> 1 -> 6  +  5 -> 9 -> 2   (617 + 295)
#   Output: 2 -> 1 -> 9                    (912)
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
from node import Node, to_list, from_list


def sum_lists(l1, l2):
    pass
