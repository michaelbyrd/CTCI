# Chapter 2 | Linked Lists
# Problem 2.3 — Delete Middle Node
#
# Implement an algorithm to delete a node in the middle (any node except the
# first and last) of a singly linked list, given only access to that node.
#
# Example:
#   Input:  a -> b -> c -> d -> e, node = c
#   Output: a -> b -> d -> e
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
from node import Node, to_list, from_list


def delete_middle_node(node):
    pass
